# Nothing should be ignored here — this repo's whole point is to ship the
# audio binaries. Listing common detritus explicitly so a stray editor
# scratch file doesn't sneak in.
.DS_Store
*.swp
*.tmp
*~

# Cles de compte de service Google (jamais versionnees)
jam-jam-*-*.json
*service-account*.json
